<div class="srm-list d-none">
    <ul class="no-list font-size-m">
        {new Array(42).fill().map( (el, index) =>
            <li class="srm-background padding-all-l" data-srm={index}>{index}</li>
        )}
    </ul>
</div>